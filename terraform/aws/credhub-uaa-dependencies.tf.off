resource "null_resource" "credhub-uaa-deploy-iaas-depedencies" {
  triggers {
    jumpbox_id     = "${local.jumpbox_id}"
    dependencies_1 = "${null_resource.credhub-uaa_scripts.id}"
  }

  depends_on = [
    "aws_elb.credhub-elb",
    "aws_elb.uaa-elb",
  ]
}
